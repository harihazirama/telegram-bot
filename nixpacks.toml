[phases.setup]
nixPkgs = ["python311", "ffmpeg"]  # This installs Python 3.11 and FFmpeg

[phases.build]
cmds = ["python -m venv /app/venv && . /app/venv/bin/activate && pip install -r requirements.txt"]
